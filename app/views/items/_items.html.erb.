<table>
  <tbody>
    <tr class="top-bar">
	    <td class="artwork">artwork</td>
	    <td class="title">title</td>
	    <td class="artist">artist</td>
	    <td class="sales-date">sales date</td>
	  </tr>
	  <% if items %>
      <% items.each do |item| %>
        <tr>
    	    <td class="item-artwork" rowspan="2"><img src="<%= item.jacket_url %>" alt="" ></td>
    	    <td class="item-title"><%= item.title %></td>
    	    <td class="item-artist"><%= item.artist %></td>
    	    <td class="item-sales-date"><%= item.release_date %></td>
    	  </tr>
    	  <tr>
    	    <td class="item-action" colspan="3">action</td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>